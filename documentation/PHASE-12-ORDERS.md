# Phase 12 — Orders

**Status:** Complete

## Features

| Route | Description |
|-------|-------------|
| `/orders` | Order history |
| `/orders/{orderNumber}` | Detail + tracking timeline |
| `/orders/{orderNumber}/invoice` | Printable HTML invoice |

## What to Test

1. Login → visit `/orders` → see past orders
2. Click **View Details** → tracking steps + status history
3. Click **Invoice** → printable page opens
4. Pending online payment → **Pay Now** link works
5. Profile page → **My Orders** button
